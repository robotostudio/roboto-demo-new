{
  "name": "roboto-demo-new",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "deploy": "turbo deploy",
    "dev": "turbo dev",
    "format": "turbo format",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "packageManager": "npm@10.5.2",
  "engines": {
    "node": ">=18"
  }
}

import { cta } from './cta';
import { hero } from './hero';
import { splitForm } from './split-form';

export const pagebuilderBlocks = [hero, cta, splitForm];

export const blocks = [...pagebuilderBlocks];
